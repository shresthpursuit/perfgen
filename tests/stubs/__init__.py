"""Stub servers for the test suite.

Test infrastructure, never part of the tool. Nothing under `perfgen/` may import from here, and
`tests/test_boundaries.py` asserts that mechanically rather than leaving it to be remembered.
"""
